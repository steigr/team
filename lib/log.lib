_log() {
  case "$1" in
    error|info|debug)
      level=$1
      shift
    ;;
    *)
      level=debug
    ;;
  esac
  echo "[$level] $*" > $(_logfile)
}

_logfile() {
  mkdir -p /run/team 2>/dev/null || true
  echo "/run/team/log"
}