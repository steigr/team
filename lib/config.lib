_defaults() {
  local serf_query_timeout="3s"
  local ssh_port="2220"
  local dokku_max_instances=3
  echo ${!1}
}

_volatile_file() {
  p="/run/$1/$2"
  mkdir -p $(dirname $p) 2>/dev/null
  echo $p
}