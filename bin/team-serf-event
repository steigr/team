#!/bin/sh

set -x

. $TEAM_LIB/team.lib
. $TEAM_LIB/serf-event.lib

serf_event_handlers | while read handler; do
  echo "Running $handler" >2
  SERF_EVENT_ENVIRONMENT=$(serf_event_environment)
  TEAM=$TEAM \
  TEAM_LIB=$TEAM_LIB \
  /bin/bash -c "$SERF_EVENT_ENVIRONMENT; $handler $*"
done