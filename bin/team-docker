#!/bin/sh

set -x

. $TEAM_LIB/team.lib
. $TEAM_LIB/docker.lib

arg=$1
shift

case "$arg" in
  bind)
    case "$1" in
      tcp)
        echo "Binding to $(docker_tcp_port)"
      ;;
    esac
  ;;
esac